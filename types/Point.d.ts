export declare class Point {
  constructor(x: number, y: number);

  /**
   * Gets point's x.
   * @returns x for this.
   */
  x(): number;

  /**
   * Gets point's y.
   * @returns y for this.
   */
  y(): number;

  /**
   * TODO: Find out.
   */
  dot(): void;
}
